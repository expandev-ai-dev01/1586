/**
 * @summary
 * Database instance configuration
 *
 * @module instances/database
 *
 * @description
 * Manages database connection instance and provides access to the connection pool.
 */

import { getPool } from '@/utils/database';

export { getPool };
